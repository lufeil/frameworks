# MyBatis
>
- MyBatis的优势在于灵活，几乎可以代替JDBC,同时提供了接口编程。MyBatis的数据访问层DAO(Data Access Objects)是不需要实现类的，只需要一个接口和XML(或者注解)。MyBatis提供自动映射、动态SQL、级联、缓存、注解、代码和SQL分离等特性，同时也可以对SQL进行优化。
>
### MyBatis核心组件
>
### MyBatis配置
>
### 映射器
>
### 动态SQL
>
### MyBatis的解析和运行原理
>
### 插件
>
